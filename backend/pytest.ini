[pytest]
testpaths = tests
asyncio_mode = auto
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    integration: requires external services (Redis/FalkorDB)
filterwarnings =
    ignore::DeprecationWarning:aiosqlite
