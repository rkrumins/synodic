# Core backend
fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0

# FalkorDB graph provider (persistent graph backend)
FalkorDB>=1.4.0
redis>=5.0

# Management DB (connection registry, ontology, views, assignment rules)
sqlalchemy>=2.0.0
aiosqlite>=0.19.0        # SQLite async driver (development)
asyncpg>=0.29.0          # PostgreSQL async driver (production)

# Security (credential encryption)
cryptography>=41.0.0

# Graph adapter dependencies
neo4j>=5.14.0            # Neo4j async driver (Phase 4)
httpx>=0.26.0            # DataHub GraphQL HTTP client + async test client

# Structured logging
python-json-logger>=2.0.7
